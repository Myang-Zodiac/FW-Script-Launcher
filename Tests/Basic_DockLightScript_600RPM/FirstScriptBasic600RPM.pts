DL_SCRIPTVERSION
1
40583


DL.ClearCommWindows				'clear window
DL.StartCommunication			'start communication


'						' how to comment code
					

DL.AddComment					' how to write to terminal
DL.AddComment "Hello World!!!!"
DL.AddComment





'~~~~~~~~~~~~~~ Pump address 4 inquire ~~~~~~~~~~~~~~~~~

DL.SendSequence "inq pump1"
result = DL.WaitForSequence ( "ACK" , 1, 5000) 		'wait 5000 millaseconds for receive sequence ACK

	if result then					'if result of wait sequence is true count + 1 then triggers if statement 
		DL.AddComment "600 RPM for 20 seconds"
		DL.SendSequence "Set Demand 600 RPM"	'send sequence Demand
		DL.Pause 500					'pause for 500 seconds for CPU and timing
		DL.SendSequence "Go"				'send sequence Go

		for I = 1 to 5				'loop for 5 iterations pause for 5 seconds within loop
			DL.Pause 5000
			DL.SendSequence "Status"		'send sequence status
		Next						'Next = end for statement 
	End if							'End if statement

DL.ResetReceiveCounter					'reset count to be used again for next inq

DL.Pause 500							'pause for 500 msec for timing


'~~~~~~~~~~~~~~ Pump address 2 inquire ~~~~~~~~~~~~~~~~~

DL.SendSequence "inq pump2"					'inq next pump
result = DL.WaitForSequence ("ACK" , 1, 5000) 

DL.ResetReceiveCounter





'~~~~~~~~~~~~~~ Pump address 3 inquire ~~~~~~~~~~~~~~~~~


DL.SendSequence "inq pump3"
result = DL.WaitForSequence ("ACK" , 1, 5000) 

DL.ResetReceiveCounter





'~~~~~~~~~~~~~~ Pump address 4 inquire ~~~~~~~~~~~~~~~~~


DL.SendSequence "inq pump4"
result = DL.WaitForSequence ("ACK" , 1, 5000) 

DL.ResetReceiveCounter







